version: 2
tag: putrefaction_works
building:
  key: putrefaction_works
  mode: REPLACE
  source: pp_building_adjustments.txt
  production_method_slots:
    - name: slot_0
      methods:
        - pp_putrefaction_works_demands
  possible_production_methods: []
  body: |2-
    is_foreign = no
    pop_type = burghers
    max_levels = manufactory_max_level
    category = basic_industry_category
    employment_size = manufactory_employment
    town = yes
    city = yes
    megalopolis = yes
    build_time = manufactory_build_time
    obsolete = saltpeter_workshop

    location_potential = {
        NOT = { raw_material = goods:saltpeter }
    }

    unique_production_methods = {
        pp_putrefaction_works_demands = {
            pottery = 0.6
            livestock = 1.2
            produced = saltpeter
            output = 2.0
            debug_max_profit = 0.75
            category = manufactory_input
        }
    }

    custom_tags = { midgame_manufactory }
    construction_demand = manufactory_construction
localization:
  entries:
    putrefaction_works_slot_0: Putrefaction Demands
    pp_putrefaction_works_demands: Putrefaction Demands

evaluation:
  building:
    employment_size: 0.2
  allow_rules:
    profit_percent: Vanilla putrefaction works economics are preserved; this replacement only adds raw-saltpeter anti-overlap.
    input_throughput: Vanilla putrefaction works economics are preserved; this replacement only adds raw-saltpeter anti-overlap.
    output_throughput: Vanilla putrefaction works economics are preserved; this replacement only adds raw-saltpeter anti-overlap.
    amortization_months: Vanilla putrefaction works economics are preserved; this replacement only adds raw-saltpeter anti-overlap.
