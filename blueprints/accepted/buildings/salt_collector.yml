version: 2
tag: salt_collector
building:
  key: salt_collector
  mode: REPLACE
  source: pp_building_adjustments.txt
  production_method_slots:
    - name: slot_0
      methods:
        - pp_salt_collector_base_salt
    - name: slot_1
      methods:
        - pp_salt_collector_salt_flats
        - pp_salt_collector_lined_pans
  possible_production_methods: []
  body: |2-
    is_foreign = no
    pop_type = laborers
    max_levels = rural_building_cap
    employment_size = 3
    category = rgo_building_category
    rural_settlement = yes
    town = yes
    city = yes
    megalopolis = yes

    location_potential = {
        OR = {
            raw_material = goods:salt
            vegetation = desert
            is_coastal = yes
        }
    }

    unique_production_methods = {
        pp_salt_collector_base_salt = {
            produced = salt
            output = 0.095
            category = building_maintenance
        }
    }
    unique_production_methods = {
        pp_salt_collector_salt_flats = {
            category = building_maintenance
        }
        pp_salt_collector_lined_pans = {
            clay = 1.200 # evaporation pan linings
            pottery = 0.261 # brine jars and settling vessels
            produced = salt
            output = 0.210
            category = building_maintenance
        }
    }

    modifier = {
    }
    build_time = rural_build_time
    construction_demand = farm_constructions
localization:
  entries:
    salt_collector_slot_0: Salt Flats
    salt_collector_slot_1: Salt Pans
    pp_salt_collector_base_salt: Salt Flats
    pp_salt_collector_salt_flats: Open Salt Flats
    pp_salt_collector_lined_pans: Lined Evaporation Pans

evaluation:
  allow_rules:
    input_throughput: Salt collection preserves the current accepted salt collector throughput below the global laborer input target.
    output_throughput: Salt collection preserves the current accepted salt collector throughput below the global laborer output target.
  production_methods:
    pp_salt_collector_base_salt:
      production_efficiency: 1.00
      allow_rules:
        base_output_per_1k: Current salt collector throughput is much smaller than the generic raw-material base band.
    pp_salt_collector_lined_pans:
      production_efficiency: 1.05
